create table `user`(
    id int(11) auto_increment primary key,
    fisrt varchar(256) not null,
    last varchar (256) not null,
    username varchar(256) not null,
    password varchar (256) not null
);